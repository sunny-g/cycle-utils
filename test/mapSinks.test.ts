/* global describe, expect, test */

import mapSinks from '../src/mapSinks';

describe('mapSinks', () => {

  test('should pick sink by string of it\'s name', () => {});

  test('should pick sinks by array of strings of their names', () => {});

  test('should pick all sinks by wildcard string \'*\'', () => {});

});

/* global describe, expect, test */

import mapSources from '../src/mapSources';

describe('mapSources', () => {

  test('should pick source by string of it\'s name', () => {});

  test('should pick sources by array of strings of their names', () => {});

  test('should pick all sources by wildcard string \'*\'', () => {});

});
